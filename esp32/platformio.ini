; ESP32-C6-LCD-1.47 Configuration
[env:esp32-c6-devkitc-1]
platform = espressif32
board = esp32-c6-devkitc-1
framework = arduino

; Serial port for uploading (adjust if needed)
; upload_port = /dev/cu.usbserial-*
; monitor_port = /dev/cu.usbserial-*
monitor_speed = 115200

; Build flags for ESP32-C6
build_flags = 
    -DCORE_DEBUG_LEVEL=3
    -DARDUINO_USB_CDC_ON_BOOT=1
    -DESP32_C6_LCD

; Required libraries
lib_deps = 
    bblanchon/ArduinoJson@^6.21.3
    links2004/WebSockets@^2.4.1
    bodmer/TFT_eSPI@^2.5.43

; Filesystem for web files
board_build.filesystem = littlefs

; Original ESP32 Configuration (for reference)
; Uncomment this section if using original ESP32 instead
; [env:esp32dev]
; platform = espressif32
; board = esp32dev
; framework = arduino
; monitor_speed = 115200
; build_flags = 
;     -DCORE_DEBUG_LEVEL=3
;     -DBOARD_HAS_PSRAM
; lib_deps = 
;     bblanchon/ArduinoJson@^6.21.3
;     links2004/WebSockets@^2.4.1
;     adafruit/Adafruit SSD1306@^2.5.7
;     adafruit/Adafruit GFX Library@^1.11.7
; board_build.filesystem = littlefs
